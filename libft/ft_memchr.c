#include "libft.h"

void    *ft_memchr(const void *s, int c, size_t n)
{
    int i;
    char *ptr;
    char *str;

    str = s;
    i = 0;
    while (i < n && str[i])
    {
        if (str[i] == )
    }
}