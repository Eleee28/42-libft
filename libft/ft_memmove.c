#include "libft.h"

void    *ft_memmove(void *dest, const void *src, size_t n)
{
    char *tmp;
    int i;

    i = 0;
    while (i++ < n)
    {
        tmp[i] = src;
    }
}

// What is the difference between memcpy and memmove??